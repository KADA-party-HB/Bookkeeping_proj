services:
  bookingservice:
    image: ghcr.io/REPLACE_WITH_GH_OWNER/dv1703-bookingservice:latest
    container_name: dv1703-bookingservice
    restart: unless-stopped
    network_mode: host
    environment:
      DATABASE_URL: ${DATABASE_URL}
